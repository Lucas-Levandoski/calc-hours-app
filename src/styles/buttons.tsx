import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  buttonPrimary: {
    borderRadius: 10,
    padding: 10,
    textAlign: 'center',
    width: '60%',
    margin: 'auto',
    marginBottom: '10%',
    fontSize: 40,
  }
});